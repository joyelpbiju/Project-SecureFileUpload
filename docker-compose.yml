version: '3.8'

services:
  secure_file_upload:
    build: .
    container_name: secure_file_upload_app
    ports:
      - "8084:8080"
    volumes:
      - ./uploads:/app/uploads
      - ./db:/app/db
    env_file:
      - .env
    restart: unless-stopped
